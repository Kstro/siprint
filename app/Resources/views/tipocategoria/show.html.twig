{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoCategoria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoCategorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoCategorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_tipocategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_tipocategoria_edit', { 'id': tipoCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
