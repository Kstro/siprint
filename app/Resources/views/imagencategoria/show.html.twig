{% extends 'base.html.twig' %}

{% block body %}
    <h1>ImagenCategoria</h1>

    <table>
        <tbody>
            <tr>
                <th>Idimagencategoria</th>
                <td>{{ imagenCategorium.idimagenCategoria }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ imagenCategorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imagencategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imagencategoria_edit', { 'id': imagenCategorium.idimagenCategoria }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
