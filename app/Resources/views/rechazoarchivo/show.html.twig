{% extends 'base.html.twig' %}

{% block body %}
    <h1>RechazoArchivo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rechazoArchivo.id }}</td>
            </tr>
            <tr>
                <th>Archivo</th>
                <td>{{ rechazoArchivo.archivo }}</td>
            </tr>
            <tr>
                <th>Motivoarchivo</th>
                <td>{{ rechazoArchivo.motivoArchivo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_disapprovedesign_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_disapprovedesign_edit', { 'id': rechazoArchivo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
