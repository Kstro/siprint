{% extends 'base.html.twig' %}

{% block body %}
    <h1>RechazoArchivo list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Archivo</th>
                <th>Motivoarchivo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rechazoArchivo in rechazoArchivos %}
            <tr>
                <td><a href="{{ path('admin_disapprovedesign_show', { 'id': rechazoArchivo.id }) }}">{{ rechazoArchivo.id }}</a></td>
                <td>{{ rechazoArchivo.archivo }}</td>
                <td>{{ rechazoArchivo.motivoArchivo }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_disapprovedesign_show', { 'id': rechazoArchivo.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_disapprovedesign_edit', { 'id': rechazoArchivo.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_disapprovedesign_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
