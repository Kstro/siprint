{% extends 'base.html.twig' %}

{% block body %}
    <h1>DetalleParametro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleParametro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ detalleParametro.nombre }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ detalleParametro.valor }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_detalleparametro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_detalleparametro_edit', { 'id': detalleParametro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
