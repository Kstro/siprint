{% extends 'base.html.twig' %}

{% block body %}
    <h1>DetalleCategoria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleCategorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ detalleCategorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detallecategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detallecategoria_edit', { 'id': detalleCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
