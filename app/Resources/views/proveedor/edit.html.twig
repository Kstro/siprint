



{% block body %}
    

{#    {{ form_start(form) }}#}
    {{ form_start(edit_form, {'action': path('admin_proveedor_edit',{'id':proveedor.id})}) }}
{#<form action="{{ path('admin_tipoparametro_new') }}" method="post" {{ form_enctype(form) }}>#}
    {{ form_errors(edit_form) }}
        <div class="row" style="margin: 50px 0;">
            <div class="col-md-5 col-xs-5 text-right">
                Company:
            </div>
            <div class="col-md-7 col-xs-7">
                {{ form_widget(edit_form.empresa) }}
            </div>
        
            <div class="clearfix"></div>
            
            
            <div class="col-md-5 col-xs-5 text-right" style="margin-top: 10px;">
                Contact:
            </div>
            <div class="col-md-7 col-xs-7" style="margin-top: 10px;">
                {{ form_widget(edit_form.contacto) }}
            </div>
        
            <div class="clearfix"></div>
            
            
            <div class="col-md-5 col-xs-5 text-right" style="margin-top: 10px;">
                Phone:
            </div>
            <div class="col-md-7 col-xs-7" style="margin-top: 10px;">
                {{ form_widget(edit_form.telefono) }}
            </div>
        
            <div class="clearfix"></div>
            
        </div>
        
            
    
        <input type="submit" value="Create" style="margin-bottom: 10px;" class="btn-nuevo btn pull-right"/>
{#</form>#}
    {{ form_end(edit_form) }}

    {#<ul>
        <li>
            <a href="{{ path('admin_tipoparametro_index') }}">Back to the list</a>
        </li>
    </ul>#}
{% endblock %}
{% block javascripts %}
    
    <script>
                $(document).ready(function(){
{#                    validarTipoParametro();#}
                    {#$('.btn-edit').on("click",function(){
                        //$(this).removeClass('btn-edit');
                    });#}
                    
                });

            </script>

{% endblock %}