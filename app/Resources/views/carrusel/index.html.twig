{% extends 'base.html.twig' %}

{% block body %}
    <h1>Carrusel list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carrusel in carrusels %}
            <tr>
                <td><a href="{{ path('admin_carousel_show', { 'id': carrusel.id }) }}">{{ carrusel.id }}</a></td>
                <td>{{ carrusel.nombre }}</td>
                <td>{% if carrusel.estado %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_carousel_show', { 'id': carrusel.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_carousel_edit', { 'id': carrusel.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_carousel_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
